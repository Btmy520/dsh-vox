# dsh-vox bundle patch: inserts its public dsh.client entry into the web
# plugin roster. The profile composes this layer through dsh.profile.bundles
# (see README for the full install steps).
- insert:
    - id: dsh-vox
      name: dsh-vox
